### Example environment variables for this project
# Copy to `.env` or `.env.production` and adjust values for your environment.

# API base URL used by the frontend to call the backend API endpoints.
# Replace with your production API host.
VITE_API_BASE=https://hotpink-tapir-344575.hostingersite.com/api

# If your backend is hosted under a sub-folder, e.g. /adminPannel, use:
# VITE_API_BASE=https://hotpink-tapir-344575.hostingersite.com/adminPannel/api

# Local development example (do NOT commit real secrets):
# VITE_API_BASE=http://localhost/adminPannel/api
